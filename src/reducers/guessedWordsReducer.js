/**
 * @function guessedWordsReducer
 * @param {array} state - Array of guessed words.
 * @param {object} action - acton to be reduced.
 * @returns {array} - new guessedWords state.
 */
export default (state, action) => {
  return null;
};
